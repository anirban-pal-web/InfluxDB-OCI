---
- name: Remove any old Influx repo
  file:
    path: /etc/apt/sources.list.d/influxdata.list
    state: absent

- name: Remove old Influx GPG key
  file:
    path: /usr/share/keyrings/influxdata-archive.key
    state: absent

- name: Install dependencies
  apt:
    name:
      - curl
      - gnupg
    state: present
    update_cache: yes

- name: Add InfluxData GPG key
  get_url:
    url: https://repos.influxdata.com/influxdata-archive.key
    dest: /usr/share/keyrings/influxdata-archive.key

- name: Add InfluxData repository
  apt_repository:
    repo: "deb [signed-by=/usr/share/keyrings/influxdata-archive.key] https://repos.influxdata.com/ubuntu jammy stable"
    state: present

- name: Install Telegraf
  apt:
    name: telegraf
    state: present
    update_cache: yes

- name: Fail if INFLUX_HOST not set
  fail:
    msg: "INFLUX_HOST environment variable is not set!"
  when: lookup('env','INFLUX_HOST') == ""

- name: Fail if INFLUX_TOKEN not set
  fail:
    msg: "INFLUX_TOKEN environment variable is not set!"
  when: lookup('env','INFLUX_TOKEN') == ""

- name: Deploy Telegraf config
  template:
    src: telegraf.conf.j2
    dest: /etc/telegraf/telegraf.conf
    owner: root
    group: root
    mode: 0644
  notify: restart telegraf

- name: Ensure Telegraf running
  service:
    name: telegraf
    state: started
    enabled: yes
